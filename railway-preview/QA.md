# 確認状況

## 実施済み
- JavaScript構文チェック（model.js / app.js）。
- `node test-model.cjs`：4コースの生成、時刻と順序の整合、保存値の上限、壊れた保存値の復旧、日付変更時リセット、線路上の座標範囲。
- HTML内のローカル画像／CSS／JSの参照先の存在確認。
- town.webp 1200×1200、328,994 bytes。train.webp 640×210、16,228 bytes。
- 外部JSライブラリ・外部フォント・実行時AIリクエストなし。

## 未確認
この環境のブラウザからローカル確認用サーバーへ接続できなかったため、ブラウザでの描画・タップ・ダイアログ・アニメーションの実動作は未確認。iPhone Safariのロード時間・フレームレート・発熱も未計測。コード検証の結果を実機性能の証拠として扱わない。

## Claude側／実機で行う確認
1. 初回は特急4駅・ぱじゃま・19:50、終点20:30が表示される。時計の短針が7と8の間、長針が10を指す。
2. 完了ボタンで1駅進む。連打しても移動中は進まない。戻すで完了数と表示が1つ戻る。
3. 全駅完了後に自動周回しない。再読込しても完了状態が復元される。
4. 親設定から10駅へ切り替わり、停車駅案内に全駅・時刻が表示される。設定を閉じただけでは保存されない。
5. 時刻だけ変更すると完了数を保持。順番の変更・コース変更では完了数が0になる。逆転した目標時刻は保存されない。
6. 画面を離れて戻る、日付が変わる、ストレージが使えない状態を確認する。
7. 375px前後のスマホ幅で文字・駅名・操作ボタンが重ならないか、拡大表示も含めて確認する。
8. 実機で初回／2回目の表示待ち、タップ直後の表示変化、電車移動を確認する。動作が重い場合は最初に画像サイズ・デコード負荷と影効果を確認し、街並みのデザイン全体を平面イラストに変更しない。

時刻・タスクは試作用の設定例。家庭での理想時刻や実績を示すものではない。

## CTO 確認(2026-09-20・Playwright/Chromium 390×844・390×660・375×550・430×932)
- 既存アプリとは別ページ・別キー(`hagu_railway_portable_v1`)。family_support の既存17キーと衝突なし。外部読み込みなし
- QA項目1〜7を機械で確認: 初回=特急4駅・ぱじゃま・19:50・終点20:30、時計の針 短針235°/長針300°(=7:50) ○/連打(同期3回)で1駅だけ進む ○/戻す ○/全駅完了でボタン無効・周回しない ○/再読込で復元 ○/10駅に切替→停車駅案内10件 ○/逆転した時刻は「駅の順番に沿って」のエラーで保存されない ○/おふとん時刻の変更は全駅を平行移動(仕様どおり)/localStorage が投げる状態でも操作できる ○/横スクロールなし・主要13要素の重なりなし・駅名20ラベルの重なり0
- **直した1件**: スマホ幅で中央の時計(120px)が先頭駅の駅名ラベルと 9〜14px かぶっていた(clock-label が隠れる)。`style.css` の @media(max-width:699px) で時計を 90px・top:49.5%(環状線の内側の空きの中心)に。修正後の最小間隔: 390=9px/375=6px/430=17px(4コース全部)。元は `style.css.orig` 相当(修正前の値: 120px・top:46%)
- 未確認(実機のみ): iPhone Safari の初回読み込み・電車の移動の滑らかさ・発熱。**K の iPhone スクショ1枚が最終関門**
- 公開の案: リポジトリ直下に `railway-preview/`(この6ファイル+assets)を追加。既存ファイルは触らない。URL は `https://baby1st-lab.github.io/hagunomachi/railway-preview/`

## CTO 追記(2026-09-20 夜・電車の脱線)
- K 指摘「左右の頂点で電車の前後が線路からはみ出る」→ **事実**。楕円の左右の頂点の曲率半径は 52.6px(390px幅)で、車体53pxの端は 6.7px 外に出る(線路の線は 5.4px)
- 直し: `train.webp` は2両連結の絵なので、**半分ずつを別々の位置・別々の接線で置く**(連結部で折れる)。中心線のずれ 6.7px → **0.85px 以下**(4駅位置で計測)。あわせて、進行方向が左向きの区間は左右反転にして**上下さかさまを解消**
- 触ったのは index.html 1行 / style.css 2行 / app.js の position()。保存データ・モデル・既存アプリには無関係(`node test-model.cjs` PASS)
- **残る(直していない)**: 真横から見た電車を真上から見た環状線に載せているので、左右の頂点では電車が縦に立つ。絵の描き方の選択なので `../RAILWAY_HANTEI_20260920.md` の A/B/C を K が決める
